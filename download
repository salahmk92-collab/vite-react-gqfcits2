import React from "react";
import ReactDOM from "react-dom/client";
import KemetConnect from "./KemetConnect.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <KemetConnect />
  </React.StrictMode>
);
